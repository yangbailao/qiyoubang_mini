function length(arr) {
  return arr.length
}
function split(str,separator) {
  console.log(str.split(','))
  return str.split(',')
}
function indexof(str,array)
{
  return array.indexOf(str)
}


module.exports = {
  split: split,
  length: length,
  indexof:indexof

}